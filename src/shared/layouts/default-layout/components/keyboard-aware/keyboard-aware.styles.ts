import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  root: {
    flexGrow: 1,
    flexDirection: 'row',
    alignItems: 'flex-end',
  },
  inner: {
    flex: 1,
    paddingBottom: 32,
  },
});
